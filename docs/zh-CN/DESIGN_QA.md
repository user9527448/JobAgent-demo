# JAI-050 设计验收

> English: [JAI-050 Design QA](../../design-qa.md)

## 输入与边界

- 规范权威：`docs/DESIGN.md`。
- 选定参考图：`docs/assets/jai-050/morning-briefing-reference.png`，`1484x1060`。
- 实现：FastAPI 在 `/app/` 同源托管的生产构建。
- 状态：只读 `FakeDashboardService` 合成固定样本。未接触业务数据库、scheduler、provider 或真实来源，
  也不提交合成预览截图。

在相同的 `1484x1060` 视口中对参考图和实现截图进行了组合对比。参考图决定构图与层级；
持久化 API 字段决定所有展示事实。

## 对比与修正

| 优先级 | 发现 | 处理 |
|---|---|---|
| P2 | 首版加入了参考图中没有的英文眉题，使主层级整体下移。 | 移除眉题，使标题、副标题和分隔线节奏与参考图对齐。 |
| P2 | 日报身份占用两行，而参考图使用一行紧凑标题。 | 合并标签与报告日期；右侧保留快照 ID 和持久化生成时间。 |
| P2 | 执行摘要区域在视觉上过浅。 | 桌面端使用有界的 168 px 最小高度恢复参考密度，窄屏仍按内容自然增长。 |

当前没有未解决的 P0、P1 或 P2 问题。

## 浏览器验证

| 视图 | 结果 |
|---|---|
| `1484x1060` 参考视口 | 顶栏、196 px 导航、阅读列、356 px 证据栏、摘要、指标、职位与证据层级符合选定构图。 |
| `1440x900` 桌面 | 保留桌面证据栏；`scrollWidth=1425 <= innerWidth=1440`；控制台无 warning/error。 |
| `1024x900` 平板 | 保留导航，证据移动到报告下方；`scrollWidth=1009 <= innerWidth=1024`。 |
| `390x844` 移动 | 单列内容和双列指标保留全部标签；`scrollWidth=375 <= innerWidth=390`。 |
| `720x900` 200% 缩放等效 | 紧凑导航与单列证据保持可用；`scrollWidth=705 <= innerWidth=720`。 |

移动导航通过 `Tab` + `Enter` 打开，在 Radix sheet 内管理焦点，并通过 `Escape` 关闭。
持久化 HTML 日报链接成功打开同源 GET 端点。来源链接有可访问名称和安全 `rel` 属性；验收期间未打开外部来源。
自动测试覆盖加载中、已载入、空数据和已脱敏错误状态。

## 闸门

final result: passed
