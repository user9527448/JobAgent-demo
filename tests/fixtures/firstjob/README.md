# Firstjob fixtures

These minimized UTF-8 JSON fixtures retain only the public job-fair fields used by the
adapter: stable ID, name, start/end dates and poster URL. Names and dates reflect public
2026 records observed on the Shanghai Student Affairs Center service; UUIDs and poster
paths are fixture-only values. No account, resume, application or personal data is stored.
